# Umami Cloud analytics. Create the site at https://cloud.umami.is, copy its
# website ID here for local runs, and set the same key in the Netlify UI under
# Site configuration -> Environment variables so production builds pick it up.
# Leave it unset and no analytics script is emitted at all.
VITE_UMAMI_WEBSITE_ID=
